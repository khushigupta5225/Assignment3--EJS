<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>Welcome <%= name %></h1>
    <h2><% if (hour < 12) { %>
        Good Morning
    <% } else if (hour < 18) { %>
        Good Afternoon
    <% } else { %>
        Good Evening
    <% } %></h2>
</body>
</html>
